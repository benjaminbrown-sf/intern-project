Elements are React components that present the most general components in the App.  These are mostly wrappers for MaterialUI components.

A few examples of elements are: Button, Input, TextArea, Table.